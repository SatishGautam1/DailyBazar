<?xml version="1.0" encoding="utf-8"?>
<!--
    IDs referenced in AddMarketItemBottomSheet.java:
      fl_image_container   → FrameLayout (tap to pick product image)
      iv_item_image        → ImageView (preview)
      til_item_name        → TextInputLayout
      et_item_name         → TextInputEditText
      til_item_price       → TextInputLayout
      et_item_price        → TextInputEditText
      til_item_category    → TextInputLayout
      et_item_category     → TextInputEditText
      switch_trend_up      → MaterialSwitch
      btn_save_item        → MaterialButton
      progress_bar         → CircularProgressIndicator
-->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingStart="24dp"
    android:paddingEnd="24dp"
    android:paddingTop="8dp"
    android:paddingBottom="32dp">

    <!-- Drag handle -->
    <View
        android:layout_width="32dp"
        android:layout_height="4dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="20dp"
        android:background="@drawable/shape_skeleton_rounded"
        android:backgroundTint="?attr/colorOutlineVariant" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="20dp"
        android:text="@string/add_market_item"
        android:textAppearance="@style/TextAppearance.Material3.TitleLarge"
        android:fontFamily="@font/montserrat_bold"
        android:textColor="?attr/colorOnSurface" />

    <!-- Product image picker -->
    <FrameLayout
        android:id="@+id/fl_image_container"
        android:layout_width="match_parent"
        android:layout_height="140dp"
        android:layout_marginBottom="20dp"
        android:background="@drawable/shape_image_picker_bg"
        android:clickable="true"
        android:focusable="true">

        <ImageView
            android:id="@+id/iv_item_image"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_storefront"
            android:contentDescription="Product image" />

        <!-- Overlay hint -->
        <LinearLayout
            android:id="@+id/ll_image_hint"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:orientation="vertical">

            <ImageView
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:src="@drawable/ic_storefront"
                app:tint="?attr/colorOnSurfaceVariant"
                android:contentDescription="Add image" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="@string/tap_to_add_image"
                android:textAppearance="@style/TextAppearance.Material3.LabelMedium"
                android:fontFamily="@font/mukta_regular"
                android:textColor="?attr/colorOnSurfaceVariant" />

        </LinearLayout>

    </FrameLayout>

    <!-- Product name -->
    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/til_item_name"
        style="@style/Widget.DailyBazar.TextInputLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="@string/product_name_hint"
        app:startIconDrawable="@drawable/ic_storefront"
        app:startIconTint="?attr/colorPrimary">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/et_item_name"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="textCapWords"
            android:imeOptions="actionNext"
            android:singleLine="true"
            android:fontFamily="@font/mukta_regular" />

    </com.google.android.material.textfield.TextInputLayout>

    <!-- Price -->
    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/til_item_price"
        style="@style/Widget.DailyBazar.TextInputLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="14dp"
        android:hint="@string/product_price_hint"
        app:startIconDrawable="@drawable/ic_trend_up"
        app:startIconTint="?attr/colorPrimary">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/et_item_price"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="text"
            android:imeOptions="actionNext"
            android:singleLine="true"
            android:fontFamily="@font/mukta_regular" />

    </com.google.android.material.textfield.TextInputLayout>

    <!-- Category -->
    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/til_item_category"
        style="@style/Widget.DailyBazar.TextInputLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="14dp"
        android:hint="@string/product_category_hint"
        app:startIconDrawable="@drawable/ic_history"
        app:startIconTint="?attr/colorPrimary">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/et_item_category"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:inputType="textCapWords"
            android:imeOptions="actionDone"
            android:singleLine="true"
            android:fontFamily="@font/mukta_regular" />

    </com.google.android.material.textfield.TextInputLayout>

    <!-- Trend switch -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/trend_up_label"
            android:textAppearance="@style/TextAppearance.Material3.BodyMedium"
            android:fontFamily="@font/mukta_medium"
            android:textColor="?attr/colorOnSurface" />

        <com.google.android.material.materialswitch.MaterialSwitch
            android:id="@+id/switch_trend_up"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:checked="true" />

    </LinearLayout>

    <!-- Progress -->
    <com.google.android.material.progressindicator.CircularProgressIndicator
        android:id="@+id/progress_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="16dp"
        android:indeterminate="true"
        android:visibility="gone"
        app:indicatorColor="?attr/colorPrimary"
        app:trackColor="?attr/colorSurfaceVariant"
        app:indicatorSize="32dp"
        app:trackThickness="3dp" />

    <!-- Save -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btn_save_item"
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:layout_marginTop="20dp"
        android:text="@string/add_market_item"
        android:fontFamily="@font/montserrat_semibold"
        style="@style/Widget.DailyBazar.Button" />

</LinearLayout>