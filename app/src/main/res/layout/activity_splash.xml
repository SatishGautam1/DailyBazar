<?xml version="1.0" encoding="utf-8"?>
<!--
    SPLASH — FrameLayout, no keyboard, no inset logic needed.
    The Splash theme sets windowBackground = brand_primary so
    there is zero white flash before this layout renders.
-->
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorPrimary">

    <!-- ── Decorative circles ───────────────────── -->
    <View
        android:layout_width="320dp"
        android:layout_height="320dp"
        android:layout_gravity="top|end"
        android:layout_marginTop="-110dp"
        android:layout_marginEnd="-110dp"
        android:background="@drawable/shape_circle_overlay" />

    <View
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:layout_gravity="bottom|start"
        android:layout_marginBottom="-70dp"
        android:layout_marginStart="-70dp"
        android:background="@drawable/shape_circle_overlay" />

    <View
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_gravity="top|start"
        android:layout_marginTop="80dp"
        android:layout_marginStart="-30dp"
        android:background="@drawable/shape_circle_overlay" />

    <!-- ── Center content ───────────────────────── -->
    <LinearLayout
        android:id="@+id/centerContent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginBottom="96dp"
        android:gravity="center"
        android:orientation="vertical">

        <!-- Logo in an elevated white circle card -->
        <com.google.android.material.card.MaterialCardView
            android:layout_width="120dp"
            android:layout_height="120dp"
            app:cardCornerRadius="60dp"
            app:cardElevation="20dp"
            app:cardBackgroundColor="@color/white"
            app:strokeWidth="0dp">

            <ImageView
                android:id="@+id/ivLogo"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:src="@drawable/logo"
                android:padding="18dp"
                android:scaleType="fitCenter"
                android:contentDescription="@string/app_name" />

        </com.google.android.material.card.MaterialCardView>

        <!-- App name using Montserrat Bold -->
        <TextView
            android:id="@+id/tvAppName"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="28dp"
            android:text="@string/app_name"
            android:textColor="@color/brand_on_primary"
            android:textSize="40sp"
            android:fontFamily="@font/montserrat_bold"
            android:letterSpacing="0.01" />

        <!-- Gold accent divider -->
        <View
            android:id="@+id/dividerGold"
            android:layout_width="52dp"
            android:layout_height="3dp"
            android:layout_marginTop="12dp"
            android:background="@color/brand_secondary" />

        <!-- Tagline using Mukta Regular -->
        <TextView
            android:id="@+id/tvTagline"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:text="@string/app_tagline_upper"
            android:textColor="@color/brand_on_primary"
            android:textSize="11sp"
            android:alpha="0.72"
            android:fontFamily="@font/mukta_regular"
            android:letterSpacing="0.10" />

    </LinearLayout>

    <!-- ── Bottom: progress + loading text ──────── -->
    <LinearLayout
        android:id="@+id/bottomSection"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:layout_marginBottom="56dp"
        android:gravity="center"
        android:orientation="vertical">

        <com.google.android.material.progressindicator.CircularProgressIndicator
            android:id="@+id/progressIndicator"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:indeterminate="true"
            app:indicatorColor="@color/brand_on_primary"
            app:trackColor="#30FFFFFF"
            app:indicatorSize="28dp"
            app:trackThickness="3dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:text="@string/splash_loading"
            android:textColor="@color/brand_on_primary"
            android:textSize="12sp"
            android:alpha="0.55"
            android:fontFamily="@font/mukta_regular"
            android:letterSpacing="0.05" />

    </LinearLayout>

</FrameLayout>